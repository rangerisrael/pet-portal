import { createGlobalState } from "react-use";

export const VetAppointmentShowForm = createGlobalState(false);
export const VetPetShowForm = createGlobalState(false);

// vet-owner
export const BranchesShowForm = createGlobalState(false);
